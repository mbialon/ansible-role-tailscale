[project]
name = "ansible-role-tailscale"
version = "0.0.0"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "ansible",
    "ansible-lint",
    "molecule>=26.8.0",
    "molecule-plugins[docker]>=26.7.15",
]

[tool.uv]
package = false
